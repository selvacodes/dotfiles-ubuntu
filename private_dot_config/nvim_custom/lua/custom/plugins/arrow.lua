return {
    "otavioschwanck/arrow.nvim",
    opts = {
        show_icons = false,
        leader_key = ';' -- Recommended to be a single key
    },
    -- config = function()
    --     vim.keymap.set("n", "<C-s>", require("arrow.persist").previous, { desc = "next arrow file"})
    -- end
}
